<UserControl x:Class="SuperDentist.App.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="24">
        <Border Style="{StaticResource Card}">
            <StackPanel>
                <TextBlock Text="Reports" Style="{StaticResource HeadingText}" />
                <TextBlock Text="Quick insights for daily operations." Style="{StaticResource SubtleText}" />

                <WrapPanel Margin="0,16,0,0">
                    <Button Content="Treatment Report" Command="{Binding ShowTreatmentReportCommand}" Width="200" Height="60" Margin="0,0,16,16" />
                    <Button Content="Patients by Doctor" Command="{Binding ShowPatientsByDoctorCommand}" Width="200" Height="60" Margin="0,0,16,16" />
                    <Button Content="Today's Appointments" Command="{Binding ShowTodayAppointmentsCommand}" Width="200" Height="60" Margin="0,0,16,16" />
                    <Button Content="Patients in Treatment" Command="{Binding ShowPatientsInTreatmentCommand}" Width="200" Height="60" Margin="0,0,16,16" />
                </WrapPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
