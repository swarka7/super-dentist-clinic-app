<UserControl x:Class="SuperDentist.App.Views.AuditHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="24">
        <Border Style="{StaticResource Card}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="360" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="220" />
                </Grid.RowDefinitions>

                <StackPanel>
                    <TextBlock Text="Audit History" Style="{StaticResource HeadingText}" />
                    <TextBlock Text="Immutable clinic change history. Times are shown in UTC and local time."
                               Style="{StaticResource SubtleText}" />
                    <ProgressBar IsIndeterminate="True"
                                 Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}" />
                </StackPanel>

                <WrapPanel Grid.Row="1" Margin="0,16,0,8">
                    <StackPanel Width="160" Margin="0,0,12,0">
                        <TextBlock Text="Entity Type" />
                        <ComboBox ItemsSource="{Binding EntityTypes}"
                                  SelectedItem="{Binding SelectedEntityType}" />
                    </StackPanel>
                    <StackPanel Width="160" Margin="0,0,12,0">
                        <TextBlock Text="Operation" />
                        <ComboBox ItemsSource="{Binding Operations}"
                                  SelectedItem="{Binding SelectedOperation}" />
                    </StackPanel>
                    <StackPanel Width="160" Margin="0,0,12,0">
                        <TextBlock Text="Actor" />
                        <TextBox Text="{Binding ActorFilter, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                    <StackPanel Width="160" Margin="0,0,12,0">
                        <TextBlock Text="Entity ID contains" />
                        <TextBox Text="{Binding EntityIdFilter, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" Margin="0,0,0,10">
                        <Button Content="Search" Command="{Binding SearchCommand}" />
                        <Button Content="Clear"
                                Command="{Binding ClearFiltersCommand}"
                                Style="{StaticResource GhostButton}"
                                Margin="8,6,0,0" />
                    </StackPanel>
                </WrapPanel>

                <DataGrid Grid.Row="2"
                          ItemsSource="{Binding Entries}"
                          SelectedItem="{Binding SelectedEntry}"
                          IsReadOnly="True"
                          SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="UTC" Binding="{Binding TimestampUtc}" Width="155" />
                        <DataGridTextColumn Header="Local" Binding="{Binding TimestampLocal}" Width="175" />
                        <DataGridTextColumn Header="Entity" Binding="{Binding EntityType}" Width="120" />
                        <DataGridTextColumn Header="Entity ID" Binding="{Binding EntityId}" Width="130" />
                        <DataGridTextColumn Header="Operation" Binding="{Binding Operation}" Width="90" />
                        <DataGridTextColumn Header="Actor" Binding="{Binding Actor}" Width="100" />
                        <DataGridTextColumn Header="Correlation ID" Binding="{Binding CorrelationId}" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>

                <TextBlock Grid.Row="3"
                           Text="Selected Change"
                           FontSize="18"
                           FontWeight="SemiBold"
                           Margin="0,16,0,8" />

                <Grid Grid.Row="4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="16" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock Text="Before" FontWeight="SemiBold" />
                    <TextBlock Grid.Column="2" Text="After" FontWeight="SemiBold" />
                    <TextBox Grid.Row="1"
                             Text="{Binding SelectedOldValues}"
                             IsReadOnly="True"
                             AcceptsReturn="True"
                             FontFamily="Consolas"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Auto" />
                    <TextBox Grid.Row="1"
                             Grid.Column="2"
                             Text="{Binding SelectedNewValues}"
                             IsReadOnly="True"
                             AcceptsReturn="True"
                             FontFamily="Consolas"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Auto" />
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
