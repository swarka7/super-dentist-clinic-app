<UserControl x:Class="SuperDentist.App.Views.TodayAppointmentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="24">
        <Border Style="{StaticResource Card}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel>
                    <TextBlock Text="Today's Appointments" Style="{StaticResource HeadingText}" />
                    <TextBlock Text="Appointments scheduled for today." Style="{StaticResource SubtleText}" />
                </StackPanel>

                <DataGrid Grid.Row="1"
                          x:Name="AppointmentsList"
                          ItemsSource="{Binding Appointments}"
                          IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Patient ID" Binding="{Binding PatientId}" Width="110" />
                        <DataGridTextColumn Header="Doctor ID" Binding="{Binding DoctorId}" Width="110" />
                        <DataGridTextColumn Header="Date" Binding="{Binding Date}" Width="110" />
                        <DataGridTextColumn Header="Time" Binding="{Binding Time}" Width="90" />
                        <DataGridTextColumn Header="Treatment" Binding="{Binding TreatmentNumber}" Width="110" />
                    </DataGrid.Columns>
                </DataGrid>

                <Button Grid.Row="2"
                        Content="Print"
                        Width="120"
                        Command="{Binding PrintCommand}"
                        CommandParameter="{Binding ElementName=AppointmentsList}" />
            </Grid>
        </Border>
    </Grid>
</UserControl>
